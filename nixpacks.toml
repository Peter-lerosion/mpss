[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["pip install fastapi uvicorn mysql-connector-python python-multipart pydantic"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"